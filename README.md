# gitstats

gitstats creates list of all open pull requests and issues for [snap](https://github.com/intelsdi-x/snap)
related repos (framework and plugins). It uses Basic Auth for GitHub authorization.

# Usage

```
$ ./gitstats -user=UserName -passwd=UserPass
```

As a result gitstats.csv is created.